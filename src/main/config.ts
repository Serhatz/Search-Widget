/**
 * Search Widget Configuration
 */
export const CONFIG = {
    // Search engine URL - query will be appended
    searchUrl: 'https://www.google.com/search?q=',

    // Global keyboard shortcut to toggle window
    // Using Alt+Space instead of Ctrl+Space to avoid Windows IME conflict
    shortcut: 'Alt+Space',

    // Window dimensions
    windowWidth: 520,
    windowHeight: 100,

    // Expanded window dimensions (for settings modal)
    expandedWidth: 520,
    expandedHeight: 500,

    // Animation duration in ms
    animationDuration: 200,

    // Dev server port
    devPort: 5174,
};
